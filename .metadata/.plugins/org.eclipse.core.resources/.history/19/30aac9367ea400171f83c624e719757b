package main.entities;

public class Names {
	String[] firstSlime = {"Gl", "Bl", "Pl", "Ga"};
	String[] endSlime = {"oorp", "oop", "omp", "urup"};
	
	public String slimeName(){
		int x = (int)(Math.random() * firstSlime.length);		
		int y = (int)(Math.random() * endSlime.length);
		ret += 
			
	}
}
